# Full local install: API + refresh (requirements-api.txt) + Streamlit dashboard.
-r requirements-api.txt

# Streamlit >=1.50 is required for the `width="stretch"` / `width="content"` API.
streamlit>=1.50,<2.0
pandas
plotly
# Optional self-hosted scheduler (scripts/scheduler.py). Production refreshes run
# in GitHub Actions (.github/workflows/kb-refresh.yml) instead.
APScheduler>=3.10,<4.0
