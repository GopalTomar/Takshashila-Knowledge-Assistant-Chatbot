// Placeholder only. frontend/build.py writes the real config.js into dist/ with the
// API_BASE_URL given at build time (Railway URL in production; for local preview:
// API_BASE_URL=http://localhost:8000 python frontend/build.py). No secrets here.
window.TK_CONFIG = { apiBaseUrl: "" };
