# Runtime dependencies for the production API + Mattermost bot (Docker image).
# No torch / sentence-transformers here: query embeddings run on the ONNX export
# of the same model (EMBEDDING_BACKEND=onnx; exported in the Dockerfile's build
# stage). Crawling and indexing run in GitHub Actions with requirements.txt.
requests>=2.31
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5
trafilatura>=1.8
ftfy>=6.1
python-dotenv>=1.0
numpy>=1.26
faiss-cpu>=1.8
onnxruntime>=1.18
tokenizers>=0.19
pymupdf>=1.24
groq>=0.9
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
cryptography>=42
tzdata
